# Dev runs against the local Firebase emulator suite (firebase emulators:start).
# Sandbox/production builds instead read .env.sandbox / .env.production; see README "Environments".
VITE_USE_EMULATOR=1
