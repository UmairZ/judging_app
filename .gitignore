# Bulky raw design exports — kept locally, not versioned.
# The extracted design source lives in docs/design/ instead.
*-handoff.zip
Ibn Katheer Developer Handoff.html
.design-handoff-extract/

# OS / editor cruft
.DS_Store
node_modules/
dist/

# Firebase emulator debug logs
*-debug.log

# Playwright / local QA scratch (screenshots + a11y snapshots)
.playwright-mcp/

# Firebase Functions
functions/node_modules/
functions/lib/
functions/.env

# Firebase deploy cache
.firebase/

# AI tooling and internal working documents
CLAUDE.md
AGENTS.md
.claude/
docs/superpowers/
oatmeal-olive-instrument.zip
oatmeal-taupe-instrument.zip
oatmeal-olive-instrument/
oatmeal-taupe-instrument/
oatmeal-taupe-mona/
/logos/
